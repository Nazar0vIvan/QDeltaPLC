import QtQml

QtObject {
  // Unit normal and offset: nx*x + ny*y + nz*z + offset = 0.
  required property real normalX
  required property real normalY
  required property real normalZ
  required property real offset
}
